import numpy as np
import matplotlib.pyplot as plt

a = np.array(range(1000))*0.005 - 2.5
x = np.array(range(1000))*0.005 + 0.1
#Ltanh = np.tanh(a)
#Lexp = 1/(1+np.exp(-a))
Llog = -(1/x)*np.log(1-x)

#plt.plot(a,Ltanh)
#plt.plot(a,Lexp)
plt.plot(a,Llog)
plt.show()
